# Pi coding agent rules

## Configuration changes

- Before changing coding-agent configuration, check `PI_CODING_AGENT_DIR` and
  use that directory. Do not assume the default `~/.pi/agent` path.
- `PI_CONFIG_DIR` is for global Pi configuration, not coding-agent files.
